node_modules
.git
.neurolink-dev/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist
# Keep GitHub Action compiled output (required for GitHub Actions)
!action-dist/

# Local action testing
.actrc
event.json

# TypeDoc generated (regenerated on each build)
docs/api/_media/

# Test Coverage
coverage/

# Test Output Files
/test-outputs/
verification-results-*.txt
feature-test-*.txt
performance-test-*.txt
# Per-test scratch dirs (must live INSIDE project so node_modules walk-up works)
.test-tmp-*/
test-results.json

# Test fixture repos created at runtime by autoresearch suite
test/.tmp-autoresearch-*

# Temp scripts written inside the project tree by suites that spawn node
# (e.g. testSchemaWithMultipleImagesSDK) so bare-import resolution finds
# project node_modules. See test/continuous-test-suite.ts.
test/.tmp/

# Verification-sweep / triage logs written by continuous-test-suite-* runs.
# Gigabyte-scale, environment-specific, never useful upstream.
test/logs/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Test Reports (generated by validation/test scripts)
test-reports/

# RAG chunking temp output
chunks.json

# Development artifacts (prevent root clutter)
*-2025-*-*-*
!docs/visual-content/screenshots/cli-screenshots/*-2025-*.png
debug-output.txt
test-output.txt
demo-results.json
batch-results.json
demo-prompts.txt
test-prompts.txt

# Allow strategic documents
!*ROADMAP-2025-2026.md

# Archive working files
archive/working/

# Visual content working files
docs/visual-content/working/

# CLI recordings working files
docs/cli-recordings/working/

# Package artifacts (keep in dist)
*.tgz

# Temporary automation files
create-*.temp.js
cli-automation-*.temp.js

# Debug and temporary files (prevent future clutter)
debug-*.js
test-*.js
test-*.mjs
test-*.ts
final-*.js
validate-*.js
quick-test.js

# JSON output files
*-validation-*.json
test-output.json
*-success-report.json

# Package lock duplicates (use pnpm only)
package-lock.json

# Testing Infrastructure Output (generated by scripts/testing/)
test-executions/
test-results/
test-outputs/
*-test-results.json
*-test-output.json
*-execution-*.log
*-test-*.log
comprehensive-test-execution-*.log
batch-test-results.json
test-batch-*.json

# Testing artifact patterns
health-reports/
*-verification-*.json
*-parallel-*.json

# Temporary test files (generated during testing)
all-tests-output.txt
test-prompts-*.txt
simple-test.txt
file.txt
greeting.txt
poem.txt
.mcp-config-test.json
test-direct-tools-temp/
test-directTools-temp/
site/
_site/
quality-metrics.json

# Test-output artifacts written by continuous-test-suite-* runs at the
# repo root (these are model outputs, not source). Pre-commit hooks
# auto-stage them otherwise.
/alphabet.txt
/city.json
/city_data.json
/city_population.json
/city_population.txt
/essay.txt
/output.txt
/hello.txt
/joke.txt
/story.txt
/poem-output.txt
/timestamp.txt

# Claude Code — local state is ignored, but .claude/skills/ is shared
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/commands/
.claude/plans/

# Security Files
*.key
*.pem
test-output/
# Generated docs search index
docs-site/static/search-index.json
.type-consolidation/
.tmp-tests/
.proof-of-work/

# Local working notes for the AI SDK removal migration (not for upstream)
memory-bank/native-runtime/

# Consumer-supplied gap/spec docs we work from locally — not part of the repo
NEUROLINK_SDK_GAPS.md
